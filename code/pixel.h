#ifndef PIXEL_H    //include guards to avoid double inclusion of library   
#define PIXEL_H    //include guards to avoid double inclusion of library

void pixel_init();
void pixel_set(uint32_t rgb);

#endif